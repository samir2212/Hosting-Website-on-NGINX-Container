apiVersion: apps/v1
kind: Deployment
metadata:
  name: mynginx-deployment
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        name: my-nginx
        image: samir2296/containerization:latest
  selector:
   matchlabels:
    app: web
